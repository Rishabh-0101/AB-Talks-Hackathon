# Frontend-safe public configuration only.
# Do NOT put private API keys or passwords in VITE_* variables.
# VITE_* values are exposed to the browser.

VITE_APP_NAME=ABTalks
VITE_API_BASE_URL=http://localhost:3000
